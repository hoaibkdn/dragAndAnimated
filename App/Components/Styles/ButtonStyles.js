import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  buttonWrapper: {
    width: 192,
    height: 40,
    overflow: "hidden",
    borderRadius: 25,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "rgb(250, 128, 80)"
  }
})
